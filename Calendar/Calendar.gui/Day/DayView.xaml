<UserControl x:Class="Calendar.gui.Day.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Calendar.gui.Day"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:DayViewModel}">
    
    <Grid Background="Aqua">
        <ItemsControl ItemsSource="{Binding PlansList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card
                        Background="{Binding BackgroundColor}"
                        Padding="8"
                        UniformCornerRadius="6">
                        <TextBlock Text="{Binding PlanEndTime}" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </materialDesign:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
